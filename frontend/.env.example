# Network label shown in sidebar
VITE_NETWORK=DevNet

# Backend API base URL — leave empty for local dev (Vite proxy handles it)
# Set this to your Railway/Render backend URL in production
# Example: VITE_API_URL=https://cantonix-backend.up.railway.app
VITE_API_URL=
